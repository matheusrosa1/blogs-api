const validateEmail = require('./validateEmail');
const validateInputNewUser = require('./validateInputNewUser');
const auth = require('./auth');
const validateCategory = require('./validateCategory');

module.exports = {
  validateEmail,
  validateInputNewUser,
  auth,
  validateCategory,
};